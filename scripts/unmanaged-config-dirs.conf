# Config dirs not managed in dotfiles but expected to exist.
# Managed dirs are detected automatically from dotfiles/.config/.
configstore
dagu
yarn
op
# holds backup.env (B2 restic secrets) — must stay out of the repo
tad
# 1Password desktop app auto-creates this (telemetry-enabled) — not user-managed
1Password
