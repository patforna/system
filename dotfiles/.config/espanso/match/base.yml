matches:
  - trigger: ":ml"
    replace: 'patric.fornasier@gmail.com'

  - trigger: ":cp"
    replace: "commit and push"

  - trigger: ":shrg"
    replace: '¯\_(ツ)_/¯'

  - trigger: ":wu"
    replace: '/wrap-up'

  - trigger: ":clip"
    replace: 'Output your response and also copy it into my clipboard'

  - trigger: ":ask"
    replace: 'If (and only if) you have questions, use the AskUserQuestion tool (multiple times, if necessary)'

  - trigger: ":ukm"
    replace: "07828268073"

  - trigger: ":cms"
    replace: "/model sonnet"

  - trigger: ":cmo"
    replace: "/model opus"

  - trigger: ":cmf"
    replace: "/model fable"

  - trigger: ":cfa"
    replace: "Proceed with full-autonomy. Don't stop or ask questions until done."

  - trigger: ":cdfb"
    replace: "please check if anything from this converation needs to flow back into the design system, style guide, decision log, skill.md or claude.md or anywhere else in this project and, if yes, integrate."
